<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Viático N° <%= viatico.numeroDeViaje %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 9pt;
            margin: 2cm;
            color: #000000;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 18pt;
        }

        .section-title {
            margin-top: 24px;
            font-weight: bold;
            font-size: 10pt;
            border-bottom: 1px solid #000;
            padding-bottom: 4px;
            margin-bottom: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 6px 8px;
            text-align: left;
        }

        p {
            margin: 6px 0;
        }
    </style>
</head>

<body>

    <h1>Solicitud de Viático Nº <%= viatico.numeroDeViaje %>
    </h1>

    <p><strong>Fecha:</strong>
        <%= new Date(viatico.fechaDeCreacion).toLocaleDateString("es-AR") %>
    </p>
    <p><strong>Área Solicitante:</strong>
        <%= viatico.areaSolicitante %>
    </p>
    <p><strong>Motivo del Viaje:</strong>
        <%= viatico.motivoDelViaje %>
    </p>
    <p><strong>Origen:</strong>
        <%= viatico.origen %> — <strong>Destino:</strong>
            <%= viatico.destino %>
    </p>

    <div class="section-title">Viajantes</div>
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Cargo</th>
                <th>Importe</th>
            </tr>
        </thead>
        <tbody>
            <% viatico.viajantes.forEach(v=> { %>
                <tr>
                    <td>
                        <%= v.nombre %>
                    </td>
                    <td>
                        <%= v.cargo %>
                    </td>
                    <td>$ <%= v.importe.toLocaleString("es-AR", {minimumFractionDigits: 2}) %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>

    <p><strong>Adicional en efectivo:</strong> $ <%= viatico.adicionalEnEfectivo.toLocaleString("es-AR") %>
    </p>
    <p><strong>Devolución:</strong> $ <%= viatico.devolucionEnEfectivo.toLocaleString("es-AR") %>
    </p>
    <p><strong>Pendiente de rendición:</strong> $ <%= viatico.pendienteDeRendicion.toLocaleString("es-AR") %>
    </p>

    <% if (viatico.valesCombustible) { %>
        <div class="section-title">Vales de Combustible</div>
        <p><strong>Cantidad:</strong>
            <%= viatico.cantidadVale %>
        </p>
        <p><strong>Valor por Vale:</strong> $ <%= viatico.valorVale.toLocaleString("es-AR") %>
        </p>
        <p><strong>Total Vales:</strong> $ <%= viatico.totalVale.toLocaleString("es-AR") %>
        </p>
        <% } %>

            <p><strong>Vehículo Utilizado:</strong>
                <%= viatico.vehiculoUtilizado %>
            </p>
            <p><strong>Creado por:</strong>
                <%= viatico.creadoPor %>
            </p>

            

</body>

</html>