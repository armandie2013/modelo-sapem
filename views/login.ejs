<div class="flex justify-center items-start w-full">
  <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-sm border border-gray-300">
    <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Iniciar Sesión</h1>

    <!-- Mensajes de error -->
    <div class="min-h-[48px] mb-6">
      <% if (errores && errores.length > 0) { %>
        <ul>
          <% errores.forEach(e => { %>
            <li class="text-red-700 text-center"><%= e.mensaje %></li>
          <% }) %>
        </ul>
      <% } %>
    </div>

    <form action="/login" method="POST" class="space-y-4">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Correo electrónico</label>
        <input type="email" name="email" id="email" required
               class="w-full border border-gray-300 px-3 py-2 rounded-lg"
               value="<%= datos?.email || '' %>">
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Contraseña</label>
        <input type="password" name="password" id="password" required
               class="w-full border border-gray-300 px-3 py-2 rounded-lg">
      </div>

      <div class="pt-4">
        <button type="submit" class="w-full bg-blue-600 text-white px-4 py-2 mt-4 rounded-lg hover:bg-blue-800 transition">
          Iniciar Sesión
        </button>
      </div>
    </form>
  </div>
</div>