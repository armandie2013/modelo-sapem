<div class="flex items-center justify-center min-h-screen bg-gray-100 border border-b">
  <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-md">
    <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">Iniciar Sesión</h1>

    <% if (errores && errores.length > 0) { %>
      <div class="bg-red-100 text-red-700 px-4 py-2 mb-4 rounded text-sm">
        <ul>
          <% errores.forEach(e => { %>
            <li><strong><%= e.campo %>:</strong> <%= e.mensaje %></li>
          <% }) %>
        </ul>
      </div>
    <% } %>

    <form action="/login" method="POST" class="space-y-4">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700">Correo electrónico</label>
        <input type="email" name="email" id="email" required
               class="w-full border px-3 py-2 rounded-lg"
               value="<%= datos?.email || '' %>">
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-700">Contraseña</label>
        <input type="password" name="password" id="password" required
               class="w-full border px-3 py-2 rounded-lg">
      </div>

      <div class="pt-4">
        <button type="submit" class="w-full bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-800 transition">
          Iniciar Sesión
        </button>
      </div>
    </form>
  </div>
</div>